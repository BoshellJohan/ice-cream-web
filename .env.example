# =============================================================
#  Plantilla de las variables que consume docker-compose.yml
#  Cópiala a `.env` y rellena los valores:  cp .env.example .env
# =============================================================

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=helados_dev

# Cadena aleatoria larga. Genera una con: openssl rand -base64 32
JWT_SECRET=
