## Qué cambia

<!-- Una o dos oraciones sobre qué hace este PR. -->

## Cómo probarlo

- [ ] `docker-compose up -d` y `cd helados-api && npm run start:dev`
- [ ] `cd helados-ui && npx ng serve`
- [ ] <!-- pasos específicos para este PR -->

## Checklist

- [ ] Tests pasan (`npm test` en helados-api)
- [ ] Build de Angular sin errores (`npx ng build --configuration=development`)
- [ ] Probado en tablet landscape (o simulado en DevTools)
